# from generator import num_atoms, num_features

# generator = Generator()
# noise = torch.randn(batch_size, 16)
# adj, features = generator(noise)

# discriminator = Discriminator(num_atoms, num_features)
# output = discriminator(adj, features)